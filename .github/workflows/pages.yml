name: Deploy docs

on:
  push:
    branches: [main]
    paths:
      # Any crate the wasm bundle depends on — cljrs-wasm transitively
      # pulls cljrs-ml, cljrs-physics, cljrs-mojo, etc. so a path filter
      # that only watches cljrs-wasm silently misses their fixes.
      - "docs/**"
      - "crates/**"
      - "src/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
      - name: Build WASM bundle
        run: |
          cd crates/cljrs-wasm
          wasm-pack build --release --target web --out-dir ../../docs/wasm
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v4
